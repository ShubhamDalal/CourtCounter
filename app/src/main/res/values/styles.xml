<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorButtonNormal</item>
    </style>

    <!-- Buttons Width Height Style. -->
    <style name="ButtonsWidthHeight">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Style For Middle Button2. -->
    <style name="ButtonsWidthHeight.Button2">
        <item name="android:layout_marginBottom">@dimen/TopBottom8dp</item>
        <item name="android:layout_marginTop">@dimen/TopBottom8dp</item>
    </style>

    <!-- Style for TeamName TextView. -->
    <style name="TeamNameTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/TopBottom16dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#616161</item>
        <item name="android:textSize">14sp</item>
        <!--<item name="app:fontFamily">sans-serif</item>-->
    </style>

    <!-- Style for TeamScore TextView. -->
    <style name="TeamScoreTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/LeftRight24dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">56sp</item>
        <!--<item name="app:fontFamily">sans-serif</item>-->
        <item name="android:typeface">normal</item>
        <item name="android:text">@string/score</item>
    </style>
</resources>